{
  "name": "borrowing-calculator-tests",
  "version": "1.0.0",
  "description": "Automated tests for the borrowing power calculator using Cucumber and Playwright",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js",
    "test:ci": "npx cucumber-js --format json:reports/results.json --format junit:reports/results.xml && node scripts/generate-html-report.js",
    "execute": "npm run test:ci"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^13.0.0",
    "multiple-cucumber-html-reporter": "^3.10.0"
  }
}
